# T-VOUCH-03 — Expired voucher → expiry error with date (SRS §10.2 / V2)

**SRS gốc:** "Expired voucher → expiry error with date." Validates **V2**. Type: Unit. SRS message
mẫu: "This voucher expired on {date}."

**Kết luận:** Pass về mặt logic (đúng code/http_status/thời điểm biên), nhưng có 1 sai khác nhỏ so
với câu chữ SRS — xem cột Ghi chú.

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                                    | Mô tả kịch bản test (Description)                                  | Dữ liệu đầu vào (Input Data)                                                             | Kết quả mong muốn (Expected Result)        | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| --- | -------------------- | ------------------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------------------------- | ------------------------------------------ | ------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_VOUCH_T03_01      | VoucherEngine — V2 date window                          | `valid_to` đã qua → phải báo hết hạn                               | `v2Window(voucher({valid_to: "2026-07-13T00:00:00Z"}), NOW)`                             | `{ok:false, code:"VOUCHER_EXPIRED"}`       | Khớp                            | Pass                | Test: `hf-medusa-store/apps/backend/src/workflows/voucher-engine/__tests__/validators.unit.spec.ts:134-140` (`it("after valid_to ⇒ EXPIRED")`). Lệnh: `pnpm test:unit -- src/workflows/voucher-engine/__tests__/validators.unit.spec.ts`. Chạy lúc 2026-07-21: 28/28 pass. **Lưu ý:** `VOUCHER_EXPIRED.customer_message` (`lib/errors.ts:50-51`) là `"Mã giảm giá đã hết hạn rồi. Bạn xem thêm mã trong \"Ví voucher\" nhé!"` — KHÔNG interpolate `{date}` như câu chữ SRS yêu cầu. Không chặn Pass (logic validate đúng), nhưng là gap nhỏ về message-contract nếu cần khớp 100% SRS. |
| 2   | TC_VOUCH_T03_02      | VoucherEngine — V2 date window (chưa tới ngày hiệu lực) | `valid_from` ở tương lai → phải báo chưa hiệu lực                  | `v2Window(voucher({valid_from: "2026-08-01T00:00:00Z"}), NOW)`                           | `{ok:false, code:"VOUCHER_NOT_YET_VALID"}` | Khớp                            | Pass                | Cùng file, `it("before valid_from ⇒ NOT_YET_VALID")`. Cùng lần chạy trên.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| 3   | TC_VOUCH_T03_03      | VoucherEngine — V2 biên (boundary)                      | `now == valid_from` và `now == valid_to` đều phải PASS (inclusive) | `v2Window(voucher({valid_from: NOW}), NOW)` và `v2Window(voucher({valid_to: NOW}), NOW)` | Cả 2 đều `{ok:true}`                       | Khớp                            | Pass                | Cùng file, `it("inclusive boundaries: now == valid_from and now == valid_to both pass")`. Cùng lần chạy trên.                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
