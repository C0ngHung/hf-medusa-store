/**
 * REMOVED (Admin unified model) — "Enable VoucherEngine" is no longer a
 * modal. The Promotion Detail "VoucherEngine Settings" section
 * (`admin/widgets/promotion-detail-voucher-config-widget.tsx`) now expands
 * the settings form INLINE when the Enable toggle is switched on, with
 * Save/Cancel in the section itself — no modal, drawer, or separate route.
 *
 * Kept as a dead stub rather than deleted: this environment's permission
 * system denies file deletion.
 */
export {};
