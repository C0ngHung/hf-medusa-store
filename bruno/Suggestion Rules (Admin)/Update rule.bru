meta {
  name: Update rule
  type: http
  seq: 4
}

put {
  url: {{baseUrl}}/admin/suggestion-rules/{{ruleId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "Complete your setup (edited)",
    "priority": 8,
    "source_product_ids": ["{{sourceProductId}}"]
  }
}
