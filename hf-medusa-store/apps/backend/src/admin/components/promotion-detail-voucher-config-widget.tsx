/**
 * RESTORED (2026-07-21) — the real widget is back at
 * `admin/widgets/promotion-detail-voucher-config-widget.tsx` (user asked to
 * remove it earlier the same session, then reversed that decision after
 * reviewing what the widget's settings actually cover). This duplicate stub
 * is dead weight left over from the removal attempt; kept rather than
 * deleted because this environment's permission system denies file deletion.
 */
export {};
