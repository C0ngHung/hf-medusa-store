# T-VOUCH-04 — Per-user limit exceeded → usage count error (SRS §10.2 / V4)

**SRS gốc:** "Per-user limit exceeded → usage count error." Validates **V4**. Type: Unit. SRS message
mẫu: "You've already used this voucher {count}/{limit} times."

**Kết luận:** Pass về logic; message không literal interpolate {count}/{limit} nhưng `details` có sẵn.

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng              | Mô tả kịch bản test (Description)                                  | Dữ liệu đầu vào (Input Data)                  | Kết quả mong muốn (Expected Result)                                               | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| --- | -------------------- | --------------------------------- | ------------------------------------------------------------------ | --------------------------------------------- | --------------------------------------------------------------------------------- | ------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1   | TC_VOUCH_T04_01      | VoucherEngine — V4 per-user limit | user_usage_count đã bằng per_user_limit → phải báo lỗi kèm details | `v4UserLimit(voucher({per_user_limit:1}), 1)` | `{ok:false, code:"VOUCHER_PER_USER_LIMIT_REACHED"}`, `details={count:1, limit:1}` | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/src/workflows/voucher-engine/__tests__/validators.unit.spec.ts:174-181` (`it("user_usage >= per_user_limit ⇒ PER_USER_LIMIT_REACHED with details")`). Impl: `lib/validators.ts` (`v4UserLimit`), `lib/errors.ts` (`VOUCHER_PER_USER_LIMIT_REACHED`, message tĩnh "Bạn đã dùng hết lượt cho mã này rồi." — không interpolate count/limit trong text, nhưng `details` object có sẵn 2 số này cho frontend tự ghép nếu cần). Lệnh: `pnpm test:unit -- src/workflows/voucher-engine/__tests__/validators.unit.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21: 28/28 pass. |
| 2   | TC_VOUCH_T04_02      | VoucherEngine — V4 dưới limit     | user_usage_count < per_user_limit → phải pass                      | `v4UserLimit(voucher({per_user_limit:3}), 2)` | `{ok:true}`                                                                       | Khớp                            | Pass                | Cùng file, `it("under per-user limit passes")`. Cùng lần chạy trên.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
