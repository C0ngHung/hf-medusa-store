# T-VOUCH-10 — Remove voucher → totals reverted, no usage increment (SRS §10.2 / VOUCH-004)

**SRS gốc:** "Remove voucher → totals reverted, no usage increment." Validates **VOUCH-004**. Type:
Integration.

**Kết luận:** Pass — đã verify live qua đúng test này khi làm [[EC-06]] (evidence dùng lại, không
chạy trùng lặp).

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                       | Mô tả kịch bản test (Description)                                                   | Dữ liệu đầu vào (Input Data)                   | Kết quả mong muốn (Expected Result)                                        | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                |
| --- | -------------------- | ------------------------------------------ | ----------------------------------------------------------------------------------- | ---------------------------------------------- | -------------------------------------------------------------------------- | ------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_VOUCH_T10_01      | VoucherEngine — Remove Voucher (VOUCH-004) | Remove voucher đã apply → cart total revert về trước khi áp, usage_count không tăng | Cart đã apply voucher trước đó trong cùng test | `updated_cart_total` = tổng trước khi áp voucher; `usage_count` không tăng | Khớp                            | Pass                | Test: `hf-medusa-store/apps/backend/integration-tests/http/apply-remove-voucher.spec.ts:330` (`it("removes an applied voucher: reverts the cart total and does not increment usage_count (tasks 3.4.2/3.4.10)")`). Lệnh: `pnpm test:integration:http -- integration-tests/http/apply-remove-voucher.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21 (đợt re-run cuối của EC-06), 8/8 suite pass, riêng test này 5819 ms. |
