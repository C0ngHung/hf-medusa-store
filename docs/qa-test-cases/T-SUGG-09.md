# T-SUGG-09 — Cart change → suggestions refresh, old cache invalidated (SRS §10.1 / SUGG-005)

**SRS gốc:** "Cart change → suggestions refresh (old cache invalidated)." Validates **SUGG-005**.
Type: Integration.

**Kết luận:** Pass — 3 test case, đã verify live khi làm [[EC-05]] (evidence dùng lại).

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                                         | Mô tả kịch bản test (Description)                                                                               | Dữ liệu đầu vào (Input Data)                              | Kết quả mong muốn (Expected Result)   | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                |
| --- | -------------------- | ------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- | ------------------------------------- | ------------------------------- | ------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_SUGG_T09_01       | SuggestiveSelling — Cache invalidation cơ chế lõi (SUGG-005) | Gọi trực tiếp `invalidateCartSuggestions` → cache phải bị xoá ngay (real cache module)                          | Seed cache key `cart:{cartId}:suggestions` dữ liệu giả    | `cache.get(key)` → `null` sau khi gọi | Khớp                            | Pass                | Test: `hf-medusa-store/apps/backend/integration-tests/http/suggestion.spec.ts:292-299`. Lệnh: `pnpm test:integration:http -- integration-tests/http/suggestion.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21 (sau fix `manage_inventory`): 9/9 pass, test này 6660 ms. |
| 2   | TC_SUGG_T09_02       | SuggestiveSelling — Subscriber `cart.updated` (SUGG-005)     | Gọi trực tiếp handler với event `cart.updated` → cache phải bị xoá                                              | Cache đã seed; event `{data:{id:CART_ID}}`                | `cache.get(key)` → `null`             | Khớp                            | Pass                | Cùng file, `suggestion.spec.ts:301-313`. Cùng lần chạy trên, test này 5841 ms.                                                                                                                                                                                             |
| 3   | TC_SUGG_T09_03       | SuggestiveSelling — Wiring Event Bus thật (SUGG-005)         | Emit `cart.updated` qua Event Bus thật (không gọi handler trực tiếp) → xác nhận subscriber thực sự nhận sự kiện | `eventBus.emit({name:"cart.updated", data:{id:CART_ID}})` | Cache bị xoá trong vòng 8000ms        | Khớp                            | Pass                | Cùng file, `suggestion.spec.ts:315-326`. Cùng lần chạy trên, test này 5770 ms.                                                                                                                                                                                             |
