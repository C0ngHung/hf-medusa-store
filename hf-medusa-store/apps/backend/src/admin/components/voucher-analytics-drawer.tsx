/**
 * REMOVED (Admin unified model) — was used only by the now-retired
 * standalone Voucher list page (`routes/promotions/vouchers/page.tsx`).
 * Voucher analytics now live inline on Promotion Detail
 * (`admin/widgets/promotion-detail-voucher-analytics-widget.tsx`).
 *
 * Kept as a dead stub rather than deleted: this environment's permission
 * system denies file deletion.
 */
export {};
