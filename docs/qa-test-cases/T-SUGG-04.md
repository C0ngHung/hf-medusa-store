# T-SUGG-04 — Suggested product out of stock → excluded (SRS §10.1 / SUGG-002(b))

**SRS gốc:** "Suggested product out of stock → excluded." Validates **SUGG-002(b)**. Type: Unit.

**Kết luận:** Pass ở tầng logic thuần (pure filter) — nhưng lưu ý gap kiến trúc đã ghi ở Step 1 audit
trước đó: check tồn kho hiện là GLOBAL, chưa scope theo "cửa hàng của khách" như SRS mô tả literal
("out of stock at the customer's assigned store"). Không lặp lại chi tiết đó ở đây.

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                                | Mô tả kịch bản test (Description)                                      | Dữ liệu đầu vào (Input Data)                                  | Kết quả mong muốn (Expected Result)                 | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------- | --------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------------------------------------- | --------------------------------------------------- | ------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_SUGG_T04_01       | SuggestiveSelling — Filter out-of-stock (SUGG-002b) | Candidate có `in_stock=false` → phải bị loại với reason `out_of_stock` | `applyBr02Filter([candidate({in_stock:false})], filterCtx())` | `kept.length=0`; `dropped[0].reason="out_of_stock"` | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/src/modules/suggestive-selling/__tests__/pipeline.unit.spec.ts:176-183` (`it("drops an out-of-stock product (b)")`). Impl: `evaluator/pipeline.ts` (`applyBr02Filter`, `isProductInStock`). Lệnh: `pnpm test:unit -- src/modules/suggestive-selling/__tests__/pipeline.unit.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21: 32/32 pass. **Lưu ý:** đây chỉ test logic filter thuần (giả lập `in_stock=false` trực tiếp) — không kiểm tra thật cơ chế tính `in_stock` theo store cụ thể, vì hiện tại `isProductInStock()` tính tồn kho toàn cục, chưa scope theo store (gap đã ghi trong Step-1 audit ban đầu). |
