# AGENTS.md

This is the pnpm workspace root, not the git root. The project's agent rules live one level
up, at the git root: **`../AGENTS.md`**. Read and follow that file.

Detailed rules are in `../.claude/rules/`; team process docs are in `../docs/team/`.
