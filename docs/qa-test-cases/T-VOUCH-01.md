# T-VOUCH-01 — Valid voucher applied → discount shown, total updated (SRS §10.2)

**SRS gốc:** "Valid voucher applied → discount shown, total updated." Validates **VOUCH-001**.
Type: Integration.

**Kết luận:** Pass — đã verify live nhiều lần trong phiên này (lần cuối trong đợt re-run cuối của
[[EC-06]], cùng file).

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                           | Mô tả kịch bản test (Description)                                                                             | Dữ liệu đầu vào (Input Data)                                                                                                                             | Kết quả mong muốn (Expected Result)                                                                                                                                      | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                             |
| --- | -------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_VOUCH_T01_01      | VoucherEngine — Apply Voucher Code (VOUCH-001) | Áp voucher percentage hợp lệ (nhập thường, code lưu hoa) lên cart 1 item → discount hiện đúng, total cập nhật | Cart: Racket 1,000,000₫ x1. Voucher `HTTPAPPLY10` (percentage, discount_value=1000=10%). Body: `{code: "httpapply10"}` (chữ thường, cố ý test normalize) | HTTP 200; `success=true`; `discount_amount=100,000`; `discount_capped=false`; `cap_explanation=null`; `updated_cart_total=900,000`; `voucher_details.code="HTTPAPPLY10"` | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/integration-tests/http/apply-remove-voucher.spec.ts:123` (`it("applies a valid voucher: carries the discount on a credit line and returns the authoritative cart total (tasks 3.4.1/3.4.4/3.4.14)")`). Lệnh: `pnpm test:integration:http -- integration-tests/http/apply-remove-voucher.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21 (đợt re-run cuối cùng của EC-06), 8/8 suite pass, riêng test này 5787 ms. |
