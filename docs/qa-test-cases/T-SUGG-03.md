# T-SUGG-03 — Suggested product already in cart → excluded (SRS §10.1 / SUGG-002(a))

**SRS gốc:** "Suggested product already in cart → excluded." Validates **SUGG-002(a)**. Type: Unit.

**Kết luận:** Pass — đã verify live khi làm [[EC-05]] (evidence dùng lại, cùng file với T-SUGG-04/05).

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                           | Mô tả kịch bản test (Description)                              | Dữ liệu đầu vào (Input Data)                                                                             | Kết quả mong muốn (Expected Result)                                    | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                    |
| --- | -------------------- | ---------------------------------------------- | -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------- | ------------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_SUGG_T03_01       | SuggestiveSelling — Filter in-cart (SUGG-002a) | Candidate đã có trong cart → phải bị loại với reason `in_cart` | `applyBr02Filter([candidate({product_id:"in_cart"})], filterCtx({cartProductIds:new Set(["in_cart"])}))` | `kept.length=0`; `dropped[0]={product_id:"in_cart", reason:"in_cart"}` | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/src/modules/suggestive-selling/__tests__/pipeline.unit.spec.ts:167-174` (`describe("applyBr02Filter — SUGG-002 (T-SUGG-03/04/05)")` → `it("drops a product already in the cart (a)")`). Impl: `evaluator/pipeline.ts` (`applyBr02Filter`, `dropReasonFor`). Lệnh: `pnpm test:unit -- src/modules/suggestive-selling/__tests__/pipeline.unit.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21: 32/32 pass. |
