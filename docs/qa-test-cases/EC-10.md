# EC-10 — Rate limit brute-force voucher (SRS §8 / SEC-02)

**SRS gốc:** "Rapid voucher code attempts (brute force)." Kỳ vọng: max 5 lần thử sai / khách /
15 phút; sau 5 lần → `429` "Too many attempts. Please try again in {minutes} minutes."; log IP +
customer_id để giám sát bảo mật.

**Priority:** Must | **Annotation trong SRS gốc:** DONE

**Kết luận:** Đúng — test dùng **Redis thật** (không phải fake-redis mặc định của integration test
env khác), tự invalidate key trước mỗi lần chạy để tránh cooldown dính từ lần chạy trước. Đã chạy
lại fresh, pass ngay.

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                                  | Mô tả kịch bản test (Description)                                                                                 | Dữ liệu đầu vào (Input Data)                                                                     | Kết quả mong muốn (Expected Result)                                                                                                                                        | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| --- | -------------------- | ----------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_VOUCH_EC10_01     | VoucherEngine — Rate limit brute-force (SEC-02/EC-10) | 5 lần liên tiếp gửi mã voucher không tồn tại → vẫn 404 từng lần. Lần thứ 6 (cùng IP+customer=null) → phải bị chặn | Cart trống. Body: `{code: "NOTREAL123"}` gửi 6 lần liên tiếp qua `POST /store/carts/:id/voucher` | 5 lần đầu: `404`. Lần 6: `429`, body khớp `{type:"rate_limited", code:"VOUCHER_RATE_LIMITED", customer_message:"Bạn đã thử quá nhiều lần. Vui lòng thử lại sau 30 phút."}` | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/integration-tests/http/voucher-rate-limit.spec.ts:81`. Impl: `api/middlewares/voucher-rate-limit.ts`, `lib/voucher-rate-limit.ts` (`recordFailedAttempt`, key = customer_id+IP composite, `logger` ghi lại mỗi lần fail — khớp yêu cầu "log IP+customer_id"), `workflows/voucher-engine/lib/rate-limit-policy.ts` (`decideRateLimit`), `modules/voucher-engine/constants.ts` (`FAIL_THRESHOLD=5`, `FAIL_WINDOW_S=900`, `COOLDOWN_S=1800`). Lệnh: `pnpm test:integration:http -- integration-tests/http/voucher-rate-limit.spec.ts` (từ `apps/backend`, cần Redis thật — `hf_medusa_redis` đang chạy). Chạy lúc 2026-07-21: **1/1 pass**, 2738 ms. |

---

## Tổng kết vòng EC-01 → EC-10

| EC    | Kết luận cuối                                                                                                                                                            |
| ----- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| EC-01 | ✅ Pass, test cũ đã đủ                                                                                                                                                   |
| EC-02 | ✅ Pass, test cũ đã đủ                                                                                                                                                   |
| EC-03 | 🔴 **Gap thật** — sàn 1 VND chưa implement, RED test đã viết, **chưa fix** (chờ quyết định)                                                                              |
| EC-04 | 🟡 **Partially done** — không có inconsistent state, nhưng không đúng "cả 2 cùng thành công" theo câu chữ SRS; **chưa fix/chưa test literal** (chờ quyết định kiến trúc) |
| EC-05 | ✅ Pass, test cũ đã đủ (phát hiện phụ: T-SUGG-01/05 fail, chưa điều tra)                                                                                                 |
| EC-06 | ✅ Pass, đã thêm 1 test mới                                                                                                                                              |
| EC-07 | ✅ Pass, đã thêm 2 test mới (route trước đó có 0% coverage)                                                                                                              |
| EC-08 | ✅ Pass, đã thêm 1 test mới — **annotation SRS "not done" sai**, nên sửa lại                                                                                             |
| EC-09 | ✅ Pass, đã thêm 1 test mới (route trước đó có 0% coverage cho case này)                                                                                                 |
| EC-10 | ✅ Pass, test cũ đã đủ                                                                                                                                                   |

**Việc còn tồn đọng, cần quyết định tiếp theo:**

1. Fix EC-03 (sàn 1 VND) — code đã biết vị trí cần sửa (`calculate-discount.ts`), RED test sẵn sàng cho bước GREEN.
2. Quyết định hướng cho EC-04 (kiến trúc lock/version) trước khi viết test/fix.
3. Điều tra T-SUGG-01/T-SUGG-05 (route `GET /store/products/:id/suggestions` trả rỗng) — phát hiện phụ từ EC-05, chưa rõ nguyên nhân.
4. Cập nhật SRS/SPEC: EC-08 annotation nên đổi "not done" → "Done".
