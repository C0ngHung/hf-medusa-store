# T-VOUCH-02 — Invalid code → specific error message (SRS §10.2 / V1)

**SRS gốc:** "Invalid code → specific error message." Validates **V1**. Type: Unit.

**Kết luận:** Pass.

| STT | Mã Test Case (TC ID) | Hạng mục / Chức năng                | Mô tả kịch bản test (Description)                                                               | Dữ liệu đầu vào (Input Data)           | Kết quả mong muốn (Expected Result)                                                                                       | Kết quả thực tế (Actual Result) | Trạng thái (Status) | Ghi chú (Notes / Error Log)                                                                                                                                                                                                                                                                                                                                                                                                                     |
| --- | -------------------- | ----------------------------------- | ----------------------------------------------------------------------------------------------- | -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- | ------------------------------- | ------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | TC_VOUCH_T02_01      | VoucherEngine — V1 exists+active    | Voucher không tồn tại (`null`) → phải trả lỗi cụ thể                                            | `v1Exists(null)`                       | `{ok:false, code:"VOUCHER_NOT_FOUND", http_status:404}`                                                                   | Khớp 100%                       | Pass                | Test: `hf-medusa-store/apps/backend/src/workflows/voucher-engine/__tests__/validators.unit.spec.ts:93-100` (`describe("VoucherEngine · V1 exists+active (3.2.4)")` → `it("null voucher ⇒ NOT_FOUND (404)")`). Impl: `lib/validators.ts` (`v1Exists`), `lib/errors.ts` (`VOUCHER_NOT_FOUND`). Lệnh: `pnpm test:unit -- src/workflows/voucher-engine/__tests__/validators.unit.spec.ts` (từ `apps/backend`). Chạy lúc 2026-07-21: **28/28 pass**. |
| 2   | TC_VOUCH_T02_02      | VoucherEngine — V1 anti-enumeration | Voucher tồn tại nhưng `is_active=false` → phải trả **CÙNG message** như not-found (chống dò mã) | `v1Exists(voucher({is_active:false}))` | `{ok:false, code:"VOUCHER_INACTIVE", http_status:422}`, `customer_message` giống hệt `VOUCHER_NOT_FOUND.customer_message` | Khớp 100%                       | Pass                | Cùng file, `it("inactive voucher ⇒ INACTIVE (422), same message as NOT_FOUND")`. Cùng lần chạy trên.                                                                                                                                                                                                                                                                                                                                            |
